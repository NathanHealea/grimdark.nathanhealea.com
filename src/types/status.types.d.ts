
/**
 * StatusType defines the different types of user statuses in the system.
 */
export type StatusType = NonNullable<'active' | 'inactive' | 'banned' | 'deleted'>;

/**
 * Status is a type that represents the status of a user in the system.
 */
export type Status = StatusType;


